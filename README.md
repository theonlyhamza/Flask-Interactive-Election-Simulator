# 🗳️ Flask Interactive Election Simulator

A web-based voting application built with Python Flask that allows users to cast votes, view real-time results, and manages candidates dynamically. Created as a 1st Semester Final Project at UET Lahore.

## 🚀 Features
* **Voter Dashboard:** Secure interface for casting votes.
* **Live Results:** Real-time updates of vote counts.
* **Admin Panel:** Manage parties and candidates dynamically.
* **Data Persistence:** Uses file-based storage/database to save votes.

## 🛠️ Tech Stack
* **Backend:** Python (Flask)
* **Frontend:** HTML, CSS, JavaScript
* **Database:** JSON / SQLite (Update this based on what you actually used)

## 📸 Usage
1. Clone the repository.
2. Install dependencies: `pip install flask`
3. Run the app: `python app.py`
